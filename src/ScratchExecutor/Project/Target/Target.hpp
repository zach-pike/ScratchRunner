#pragma once

#include <string>
#include <map>

#include "Variable.hpp"

class Varia

class Target {
private:
    bool isStage;
    std::string name;

    // Maps variable id's to a variable
    std::map<std::string, Variable>

public:
    Target();
    ~Target();
};